// src/characters/Pip.js — Phase 1 stub.
// Pip the fairy guide/companion. A procedural Pip is drawn inline in MainMenu
// for Phase 1; this class will own his reusable sprite + voiced lines later.

export default class Pip {
  // TODO(phase 2): reusable Pip sprite, animations, and dialogue.
}
