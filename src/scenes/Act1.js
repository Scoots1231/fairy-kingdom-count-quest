// src/scenes/Act1.js — Phase 1 stub.
import { buildStub } from '../ui/stub.js';

export default class Act1 extends Phaser.Scene {
  constructor() { super('Act1'); }
  create() {
    buildStub(this, { title: 'Act 1 — Coming Soon', subtitle: 'The Whispering Forest' });
  }
}
