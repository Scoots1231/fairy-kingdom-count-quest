// src/scenes/Act2.js — Phase 1 stub.
import { buildStub } from '../ui/stub.js';

export default class Act2 extends Phaser.Scene {
  constructor() { super('Act2'); }
  create() {
    buildStub(this, { title: 'Act 2 — Coming Soon', subtitle: 'The Murky Swamp' });
  }
}
