// src/scenes/Act3.js — Phase 1 stub.
import { buildStub } from '../ui/stub.js';

export default class Act3 extends Phaser.Scene {
  constructor() { super('Act3'); }
  create() {
    buildStub(this, { title: 'Act 3 — Coming Soon', subtitle: 'The Sunlit Fields' });
  }
}
