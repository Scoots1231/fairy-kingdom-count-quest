// src/scenes/Act4.js — Phase 1 stub.
import { buildStub } from '../ui/stub.js';

export default class Act4 extends Phaser.Scene {
  constructor() { super('Act4'); }
  create() {
    buildStub(this, { title: 'Act 4 — Coming Soon', subtitle: 'The Crystal Castle' });
  }
}
