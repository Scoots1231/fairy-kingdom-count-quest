// src/scenes/Shop.js — Phase 1 stub.
import { buildStub } from '../ui/stub.js';

export default class Shop extends Phaser.Scene {
  constructor() { super('Shop'); }
  create() {
    buildStub(this, { title: "Pip's Shop — Coming Soon", subtitle: 'Spend your fairy dust here later!' });
  }
}
