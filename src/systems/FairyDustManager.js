// src/systems/FairyDustManager.js — Phase 1 stub.
// Tracks fairy dust balance + lifetime total, syncing through SaveSystem.
// To be implemented in a later phase.

export default class FairyDustManager {
  // TODO(phase 4): add(amount), spend(amount), getBalance().
}
