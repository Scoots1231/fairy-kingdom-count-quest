// src/systems/ProceduralGenerator.js — Phase 1 stub.
// Generates encounter layouts (counts, item placements, distractors) per act.
// To be implemented in a later phase.

export default class ProceduralGenerator {
  // TODO(phase 4): generate(actKey, difficulty) -> encounter definition.
}
