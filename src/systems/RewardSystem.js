// src/systems/RewardSystem.js — Phase 1 stub.
// Resolves rewards (fairy dust, items, pity-system payouts) after encounters.
// To be implemented in a later phase.

export default class RewardSystem {
  // TODO(phase 4): resolve(actKey, result) -> rewards.
}
