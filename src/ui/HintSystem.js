// src/ui/HintSystem.js — Phase 1 stub.
// Adaptive hint prompts during encounters (escalates on repeated misses).
// Later phase.

export default class HintSystem {
  // TODO(phase 4): show graduated hints tied to encounter state.
}
