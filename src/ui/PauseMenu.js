// src/ui/PauseMenu.js — Phase 1 stub.
// In-game pause overlay (resume, save, quit to menu). Later phase.

export default class PauseMenu {
  // TODO(phase 2): overlay with resume/save/quit, keyboard + mouse nav.
}
